# B站与燕云十六声资讯监控推送系统方案

## 1. 文档目的

本文档用于确定项目第一阶段的业务范围、技术架构、数据模型、可靠性策略和实施顺序。

当前仓库已经包含第一版可运行骨架和核心业务实现。后续扩展必须以本文档和 `AGENTS.md` 为边界。

## 2. 需求理解

系统需要同时监控两类公开资讯来源：

1. 指定 B 站 UP 主的最新动态。
2. 燕云十六声官网新闻页面及其文章详情。

发现新内容后，系统需要：

1. 获取结构化内容或文章正文。
2. 转换为统一内容模型。
3. 使用本地图片模板生成完整图片。
4. 通过 QQ 官方机器人 API v2 发送图片消息。
5. 在管理页面配置 UP 主、QQBot 凭据、推送目标和轮询规则。

## 3. 总体结论

采用模块化单体架构，分为 Web 管理进程和轮询/推送 Worker 两个运行角色，共享同一套代码和数据库。

初期使用 SQLite，不引入 Redis、Kafka 或微服务。项目规模扩大后，再将数据库替换为 PostgreSQL，并按需要增加独立队列。

```text
浏览器管理页面
        │
        ▼
FastAPI 管理服务 ───── SQLite
        │                  │
        └────────────── Worker
                           │
             ┌─────────────┴─────────────┐
             ▼                           ▼
       B站轮询采集器                 官网轮询采集器
             │                           │
             └──────────┬────────────────┘
                        ▼
                  统一内容模型
                        │
                 去重与事务入库
                        │
                   Outbox 推送队列
                        │
                 图片渲染与媒体上传
                        │
                 QQ 官方 Bot API v2
```

## 4. B站采集方案

### 4.1 处理原则

B站不访问动态网页进行截图，也不依赖 B 站页面 DOM。

正确流程是：

```text
bilibili-api-python 获取结构化数据
        ↓
解析动态类型和字段
        ↓
转换为 BiliDynamicViewModel
        ↓
套用本地 HTML 模板
        ↓
Playwright 截图生成 PNG
        ↓
QQBot 富媒体发送
```

模板视觉可以参考 B 站动态页，但模板和 CSS 由本项目维护，不复制 B 站网页源码，也不让业务逻辑依赖 B 站网页结构。

### 4.2 参考实现中可借鉴的内容

参考项目：

- [Soulter/astrbot_plugin_bilibili](https://github.com/Soulter/astrbot_plugin_bilibili)
- [bili_client.py](https://raw.githubusercontent.com/Soulter/astrbot_plugin_bilibili/master/bili_client.py)
- [services/listener.py](https://raw.githubusercontent.com/Soulter/astrbot_plugin_bilibili/master/services/listener.py)
- [core/models.py](https://raw.githubusercontent.com/Soulter/astrbot_plugin_bilibili/master/core/models.py)
- [services/subscription_service.py](https://raw.githubusercontent.com/Soulter/astrbot_plugin_bilibili/master/services/subscription_service.py)

可借鉴：

1. 使用独立 `BiliClient` 集中封装 `Credential` 和 API 调用。
2. 通过 `user.User(uid).get_dynamics_new()` 获取指定 UP 主动态。
3. 按 UID 合并请求，同一个 UP 主在一个轮询周期内只请求一次。
4. 使用动态 `id_str` 作为增量检测依据。
5. 保存 `last_id` 和有限的 `recent_ids`，避免重复处理。
6. 订阅首次建立时初始化游标，默认不补发历史动态。
7. 按动态类型分别处理视频、图文、文字、专栏和转发动态。
8. 使用统一渲染模型，再交给模板渲染器处理。

不复制：

- AstrBot 指令系统。
- AstrBot 会话 UMO。
- `MessageChain`、`At`、`AtAll` 等机器人内部消息对象。
- AstrBot 的图片渲染和消息分发实现。
- 直播监控逻辑。

### 4.3 B站模块边界

```text
collectors/bilibili/
├── client.py          # bilibili-api-python 的唯一适配入口
├── poller.py          # 按 UID 调度轮询
├── parser.py          # 原始动态解析和类型分派
├── view_models.py     # 渲染模型
├── asset_cache.py     # 图片、封面资源缓存
├── renderer.py        # 选择本地图片模板并生成 PNG/JPG
└── templates/
    ├── video.html
    ├── draw.html
    ├── word.html
    ├── article.html
    ├── forward.html
    └── fallback.html
```

### 4.4 B站渲染模型

```text
BiliDynamicViewModel
├── dynamic_id
├── dynamic_type
├── uid
├── author_name
├── avatar_url
├── publish_time
├── title
├── content
├── images
├── video_cover
├── video_url
├── forward_content
└── source_url
```

动态类型至少支持：

- `DYNAMIC_TYPE_AV`：视频动态。
- `DYNAMIC_TYPE_DRAW`：图文动态。
- `DYNAMIC_TYPE_WORD`：文字动态。
- `DYNAMIC_TYPE_ARTICLE`：专栏动态。
- `DYNAMIC_TYPE_FORWARD`：转发动态。

未知类型必须保存原始数据，并使用通用模板输出作者、类型、文本、动态 ID 和原始链接，不能静默丢弃。

### 4.5 B站凭据

B站动态接口可能需要登录凭据。管理页面需要单独配置并加密保存：

- `SESSDATA`。
- `BILI_JCT`。
- `BUVID3`。
- 其他库实际需要的 Credential 字段。

B站凭据与 QQBot 的 `AppSecret` 分开管理，页面只显示脱敏值。第一阶段实现本项目自己的扫码登录和凭据管理流程，但不复制 AstrBot 的机器人指令、会话和消息分发层。

### 4.6 B站扫码登录和凭据生命周期

扫码登录属于 B站采集基础设施，不属于 QQBot 推送逻辑。流程如下：

```text
管理页面点击扫码登录
        ↓
后端生成二维码并返回短期展示信息
        ↓
用户使用 B站 App 扫码
        ↓
后端轮询登录状态
        ↓
提取 Credential 并加密持久化
        ↓
BiliClient 重新加载登录态
```

登录模块需要维护以下状态：

- `not_configured`：尚未配置登录态。
- `waiting_scan`：二维码已生成，等待扫码。
- `logged_in`：凭据有效。
- `refreshing`：正在刷新凭据。
- `relogin_required`：凭据失效，需要重新扫码。
- `failed`：登录或刷新发生异常。

运行规则：

1. 未配置凭据时暂停 B站轮询，不循环刷错误日志。
2. 凭据有效时正常执行动态轮询。
3. 后台低频检查凭据是否需要刷新。
4. 刷新成功后原子保存新凭据，避免内存和数据库状态不一致。
5. 凭据失效时停止 B站采集并在管理页面提示重新扫码。
6. 发生 412 或频率异常时采用退避，不立即反复触发扫码登录。

第一阶段只支持单个 B站账号凭据，所有 UP 主共用该凭据。后续如有多账号需求，再扩展凭据与订阅的关联关系。

## 5. 官网采集方案

官网当前新闻页存在“最新、新闻、公告、活动”等分类，并使用列表、分页和文章详情页组织内容。

官网采集不依赖内部接口，采用公开 HTML 页面：

```text
请求新闻列表页
        ↓
提取标题、分类、日期、文章 URL
        ↓
根据 URL 去重
        ↓
请求新文章详情页
        ↓
提取标题、正文、图片和日期
        ↓
保存原始 HTML 与清洗后的正文
```

官网模块边界：

```text
collectors/yysls/
├── client.py          # httpx 请求
├── list_parser.py     # 列表和分页解析
├── article_parser.py  # 文章详情解析
├── selectors.py       # 可集中调整的页面选择器
└── fixtures/          # HTML 解析测试样本
```

官网列表和正文使用公开 HTML 采集；图片推送时优先打开文章详情页，定位 `#NIE-art` 内容容器并直接截图，从而保留官网原始 CSS 和图片布局。官网页面不可访问或选择器失效时，再使用本地 HTML 模板截图。两条路径统一经过 Playwright，不维护 Pillow 业务渲染分支。

## 6. 图片生成方案

### 6.1 渲染工具

使用 Playwright 作为唯一图片渲染引擎。它是项目内按需启动的本地无头浏览器进程，不是需要单独部署的渲染服务；截图完成后立即关闭。B站的 HTML 来自本地模板，官网优先使用真实详情页的 `#NIE-art` 容器。

B站：

```text
结构化 API 数据 → 本地 HTML 模板 → Playwright PNG
```

官网：

```text
文章 URL → 详情页 `#NIE-art` → Playwright 截图
             ↘ 失败 → 本地 HTML 模板 → Playwright 截图
```

### 6.2 资源缓存

B站动态中的头像、封面和图片建议先下载到本地缓存，再供模板引用。这样可以避免远程图片加载失败造成截图不完整。

缓存文件使用内容哈希命名，并设置大小和过期清理策略。

### 6.3 长图处理

文章或动态过长时：

- 优先生成完整长图。
- 超过内部高度或文件大小阈值时自动分页。
- 图片底部显示页码。
- 最后一页保留原文链接。
- 图片渲染失败时回退到 Markdown 或纯文本推送。

默认优先 PNG，超过文件大小阈值时转 JPG。文字内容需要保证清晰度，不能为了压缩过度降低可读性。

## 7. QQBot 官方 API v2 接入

QQBot 不是普通的自定义 Webhook，采用 QQ 官方 OpenAPI：

- API 基础地址：`https://api.bot.qq.com`。
- 用户配置：`AppID`、`AppSecret`。
- 程序自动获取和刷新 `AccessToken`。
- 请求头：`Authorization: QQBot ACCESS_TOKEN`。

参考：

- [启动接入](https://bot.q.qq.com/wiki/develop/api-v2/)
- [获取访问凭证](https://bot.q.qq.com/wiki/develop/api-v2/dev-prepare/access-token.html)
- [API调用指南](https://bot.q.qq.com/wiki/develop/api-v2/dev-prepare/api-call-guide.html)
- [消息收发概述](https://bot.q.qq.com/wiki/develop/api-v2/server-inter/message/overview.html)

### 7.1 推送目标

第一阶段支持：

- 群聊：`group_openid`。
- 单聊：`user_openid`。

不直接使用普通 QQ 群号或 QQ 号。目标配置需要保存场景类型和对应 OpenID。

### 7.2 图片发送

QQ 官方图片消息使用 `msg_type=7`，发送前需要上传文件获得 `file_info`。

```text
本地 PNG/JPG
   ↓
按目标场景上传
   ↓
获取 file_info
   ↓
发送 msg_type=7 图片消息
```

单聊和群聊的上传接口相互隔离，`file_info` 也有有效期。因此 `file_info` 不作为长期业务数据保存，推送重试时重新上传。

QQBot 推送模块：

```text
delivery/qqbot/
├── auth.py
├── client.py
├── media_uploader.py
├── message_sender.py
├── limiter.py
└── errors.py
```

### 7.3 推送格式

默认模式：

```text
图片消息
```

备用模式：

```text
Markdown → 纯文本
```

如果图片发送失败，系统记录错误并按策略重试；多次失败后，允许在管理页面手动使用 Markdown 或纯文本补发。

## 8. 核心数据模型

### `bilibili_subscription`

```text
id
uid
display_name
enabled
poll_interval_seconds
filter_types
filter_keywords
last_dynamic_id
recent_dynamic_ids
next_poll_at
last_success_at
last_error
```

### `watch_source`

```text
id
source_type          # bilibili / yysls
name
url
category
enabled
poll_interval_seconds
next_poll_at
```

### `content_item`

```text
id
source_type
source_key
external_id
title
author
category
published_at
content_text
content_html
render_payload_json
raw_payload_json
source_url
content_hash
created_at
```

唯一约束：

```text
UNIQUE(source_type, source_key, external_id)
```

### `delivery_target`

```text
id
scene_type           # group / user
target_openid
enabled
message_mode        # image / markdown / text
render_mode         # playwright，统一使用 Playwright
```

### `delivery_task`

```text
id
content_item_id
delivery_target_id
status               # pending / processing / sent / retry / failed
retry_count
next_retry_at
qq_message_id
qq_trace_id
last_error
created_at
sent_at
```

## 9. 可靠性设计

使用 Outbox 模式：

```text
采集成功
  ↓
内容和推送任务同事务入库
  ↓
更新采集游标
  ↓
Worker 异步渲染、上传和发送
```

这样 QQBot 暂时不可用时，已采集内容不会丢失。

重试建议：

```text
1分钟 → 5分钟 → 30分钟 → 2小时
```

以下情况通常不应无限重试：

- QQBot 凭据错误。
- OpenID 不存在或无权限。
- 图片格式或消息内容不合法。
- 机器人没有目标群/用户的发送权限。

临时网络异常、HTTP 429 和 HTTP 5xx 可以重试。

## 10. 管理页面

### 首页

- 采集器运行状态。
- 最近轮询时间。
- 最近发现内容。
- 待推送数量。
- 失败推送数量。

### B站 UP主管理

- UID 添加和删除。
- 自动查询 UP 主名称。
- 启用/停用。
- 动态类型过滤。
- 关键词过滤。
- 轮询间隔。
- 当前游标和最近动态。
- 测试采集。

### 官网来源管理

- 最新/新闻/公告/活动分类。
- 自定义列表 URL。
- 轮询间隔。
- 本地图片模板模式。
- 测试解析。

### QQBot 配置

- AppID。
- AppSecret。
- 群聊或单聊目标。
- 目标 OpenID。
- 图片/Markdown/文本模式。
- 测试发送。
- AccessToken 有效状态。

### B站账号配置

- 当前登录状态。
- 开始扫码登录。
- 二维码展示和轮询状态。
- Credential 有效性测试。
- 自动刷新状态。
- 退出登录和重新扫码。
- 手动录入 Cookie 的备用入口。

### 内容和推送记录

- 已采集内容。
- 原始内容预览。
- 图片预览。
- 已发送/待重试/失败记录。
- 手动重试和补发。

## 11. 安全要求

1. AppSecret、B站 Cookie、加密密钥不能写入代码仓库。
2. 管理页面必须有登录保护。
3. 密钥页面只显示脱敏值。
4. 日志禁止输出完整 Token、Cookie 和 Secret。
5. 图片渲染统一使用 Playwright；B站不执行来源页面脚本，官网只在公开详情页截图指定内容容器，不执行站外注入代码。
6. 不实现 DLL 注入、证书绕过、反爬绕过或游戏客户端拦截。
7. B站请求保持低频，默认轮询间隔不低于 5 分钟。

## 12. 默认业务规则

第一版采用以下默认值：

- B站轮询间隔：5 分钟。
- 官网轮询间隔：10 分钟。
- 首次添加来源：只建立基线，不推送历史内容。
- B站：API 数据生成本地 HTML 模板，再由 Playwright 截图。
- 官网：优先直接截图详情页 `#NIE-art`，失败时使用同一 Playwright 引擎截图本地模板。
- 推送：优先图片，失败后 Markdown，最后纯文本。
- 时区：Asia/Shanghai。
- 初期只支持一个 QQBot AppID，可配置多个推送目标。

## 13. 实施顺序

### 阶段一：基础骨架

- Python 项目和配置系统。
- SQLite 数据库。
- FastAPI 管理服务。
- Worker 启动和健康状态。

### 阶段二：B站采集和渲染

- 实现 B站扫码登录和 Credential 加密保存。
- 实现登录态检查、刷新和重新登录提示。
- 封装 `BiliClient`。
- 实现 UID 轮询、游标和去重。
- 实现五类动态解析。
- 实现本地 HTML 模板并接入 Playwright 截图。

### 阶段三：官网采集和渲染

- 列表页解析。
- 文章详情解析。
- 正文和图片提取。
- 官网 `#NIE-art` 容器截图和本地 HTML 模板兜底。

### 阶段四：QQBot 图片发送

- AppID/AppSecret 鉴权。
- AccessToken 缓存和刷新。
- 群聊/单聊图片上传。
- 图片消息发送和错误分类。

### 阶段五：管理页面和可靠性

- 来源配置页面。
- QQBot 目标配置页面。
- 推送记录和重试。
- 图片预览。
- Docker 部署和完整联调。

## 14. 验收标准

1. 新增一个 B 站 UID 后可以测试获取动态。
2. 管理页面可以完成 B站扫码登录并显示登录状态。
3. B站凭据失效后能够提示重新扫码，不会导致 Worker 高速重试。
4. B站动态可以使用本地模板生成完整图片。
5. 官网新文章可以提取正文并生成图片。
6. 同一动态或文章重复轮询不会重复入库。
7. 首次启动不会批量推送历史内容。
8. QQBot 图片发送失败后能够重试。
9. QQBot 图片上传按群聊/单聊目标正确隔离。
10. 服务重启后游标、推送队列和失败记录仍然保留。
11. 管理页面不会显示完整 AppSecret、AccessToken 或 B站 Cookie。
